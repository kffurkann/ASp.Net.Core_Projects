@model ProductListViewModel//IEnumerable<ProductViewModel> yazmak yerine

@foreach (var p in Model.Products)//listview daki Products
{
    <div>
        <h4>@p.Name</h4>
        <p>@p.Description</p>
        <p>@p.Price</p>
    </div>
}
<h5>Index Page</h5>
